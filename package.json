{
  "name": "birdseye-theme-harmony",
  "version": "0.0.5",
  "description": "birdseye-frontend's default theme",
  "main": "index.js",
  "scripts": {
    "build": "gulp build",
    "watch": "gulp watch",
    "lint": "stylelint 'src/**/*.{css,scss}'",
    "fix": "stylelint --fix 'src/**/*.{css,scss}'",
    "ver:patch": "npm version patch && yarn build",
    "ver:minor": "npm version minor && yarn build",
    "ver:major": "npm version major && yarn build"
  },
  "repository": "https://github.com/MarinDeck/birdseye-theme-harmony.git",
  "author": "hakunagi <hakunagi.rei@gmail.com>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-dart-sass": "^1.0.2",
    "gulp-header": "^2.0.9",
    "gulp-rename": "^2.0.0",
    "gulp-sass-glob-use-forward": "^0.1.3",
    "postcss": "8",
    "stylelint": "^14.6.1",
    "stylelint-config-recess-order": "^3.0.0",
    "stylelint-config-standard-scss": "^3.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
